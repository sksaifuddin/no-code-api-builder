import React from 'react';
import './Header.scss';

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">No Code API Builder</h1>
    </header>
  );
};

export default Header;
